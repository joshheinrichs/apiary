[package]
name = "device-dumper"
version = "0.1.0"
edition = "2024"

[dependencies]
libdisplay-info = { version = "0.3", features = ["v0_3"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
